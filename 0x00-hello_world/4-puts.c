#include <stdio.h>
/*
 * main - main marks the entry point into the program
 * @void: When parameter X is void, it means it is not accepting any args
(*
 * Description: Uses puts to print strings only as standard output)?
(* stdio header: Contains function puts)*
 * Return: returned value is 0 which means the execution was successful
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
